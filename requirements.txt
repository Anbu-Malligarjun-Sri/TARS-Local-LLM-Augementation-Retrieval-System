# TARS Chatbot v2 - Enhanced Architecture
# Minimal dependencies for FastAPI backend runtime on Vercel.
# Optional RAG/ML packages can be installed separately for local use.

# LLM Providers
openai>=1.12.0
google-generativeai>=0.4.0

# Voice (optional; Windows-focused local feature)
pyttsx3>=2.90; platform_system == "Windows"
SpeechRecognition>=3.10.0; platform_system == "Windows"
pyaudio>=0.2.14; platform_system == "Windows"

# Backend API
fastapi>=0.109.0
uvicorn>=0.27.0
python-multipart>=0.0.6
websockets>=12.0

# Data Processing
pyyaml>=6.0.1
python-dotenv>=1.0.0
pydantic>=2.5.0
pydantic-settings>=2.1.0

# NLP
nltk>=3.8.1
tiktoken>=0.5.2

# Utilities
rich>=13.7.0
typer>=0.9.0

# Testing (optional)
pytest>=7.4.0; extra == "dev"
pytest-asyncio>=0.23.0; extra == "dev"

# Development (optional)
black>=24.1.0; extra == "dev"
isort>=5.13.0; extra == "dev"
